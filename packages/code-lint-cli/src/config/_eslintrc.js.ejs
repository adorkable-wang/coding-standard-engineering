module.exports = { extends: [ '<%= eslintType === 'index' ? 'adorkable-wang-eslint-config' :
`adorkable-wang-eslint-config/${eslintType}` %>', <%_ if (enablePrettier) { _%> 'prettier', <%_ } _%> ],
};
